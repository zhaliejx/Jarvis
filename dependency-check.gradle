apply plugin: 'org.owasp.dependencycheck'

dependencyCheck {
    failOnError = false
    suppressionFile = 'config/quality/dependency-suppression.xml'
    analyzers {
        retirejs.enabled = false
    }
}